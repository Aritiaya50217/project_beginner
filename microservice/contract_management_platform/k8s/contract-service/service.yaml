apiVersion: v1
kind: Service
metadata:
  name: contract-service
  namespace: contract-platform
spec:
  selector:
    app: contract-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
