apiVersion: v1
kind: Service
metadata:
  name: notification-worker
  namespace: contract-platform
spec:
  selector:
    app: notification-worker
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
