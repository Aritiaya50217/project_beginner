apiVersion: v1
kind: Service
metadata:
  name: contract-proxy
  namespace: contract-platform
spec:
  selector:
    app: contract-proxy
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
